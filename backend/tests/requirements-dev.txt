# Test-only dependencies (NOT bundled into the Lambda — see src/requirements.txt).
# Used to forge Apple-style RS256 tokens and run the suite:
#   python3.13 -m venv .venv && . .venv/bin/activate
#   pip install -r tests/requirements-dev.txt && pytest
pytest>=8
PyJWT[crypto]>=2.8,<3
# boto3 ships in the Lambda runtime (so it's NOT in src/requirements.txt), but
# tests run outside that runtime. apns.device_tokens imports
# boto3.dynamodb.conditions.Key, so the suite needs boto3 locally to exercise it.
boto3>=1.34
