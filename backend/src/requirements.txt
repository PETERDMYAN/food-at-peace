# Bundled into every Lambda (shared CodeUri). Kept to PURE-PYTHON wheels only, so
# `sam build` needs no Docker / cross-compilation and stays portable to the Lambda
# Linux runtime.
#
# Used by apns.py (Circle + Posts push):
#   httpx[http2] — HTTP/2 client (APNs is HTTP/2-only); pulls h2/hpack/hyperframe.
#   ecdsa        — pure-Python ES256 signing of the APNs provider JWT (avoids the
#                  native `cryptography` wheel, which can't cross-compile here).
# Everything else (analyze/auth/sync/account/circle/beans/iap) uses only the
# standard library + boto3 (already in the runtime); JWT for our own tokens +
# Apple's RS256 identity tokens is done in jwtlite.py (stdlib).
httpx[http2]
ecdsa
